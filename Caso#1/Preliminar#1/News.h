#include <iostream>
#include<string>
 
void getTopFive()
{
    //Muestra las primeras cinco noticias que se encuentren en la lista.
    //printTopFive();
    //findPosition();
}

void getAll()
{
    //Muestra toda la lista.
    //printList();
    //findPosition();
}

void relocate(int/*si vamos a usar posicion para seleccionar*/ string/*si vamos a usar una palabra o nombre especifico*/ dataAMover, int cuantoSeQuiereMover)
{
    //Mover una noticia a otra posicion en la lista.
    //      find(string pNoticia) si quiere mover un titulo especifico
    //      findByPosition(int position) si quiere mover un indice en especifico
    //      changePosition(int newsToRelocate, int yUp_or_yDown)
}

void deleteIndex(int index)
{
    //Borra el indice indicado
    //   deleteByPosition();
}

void deleteByName(string newsName)
{
    //Borra el la noticia del nombre indicado
    //   deleteByName();
}

void addHeadline()
{
    //Agregar datos a la lista
    //  add(string pData);
}

void addHeadlineInPosition(int position);
{
    //Agrega un headline en una posicion especifica
    //   insert(string pData, int position);
}